/**
 * Transformation UI components module
 * Handles UI components specific to transformation blocks and interfaces
 * @module mapping/ui/transformation-ui
 */

// Import dependencies
import { createElement, createButton } from '../../ui/components.js';
import { BLOCK_TYPES, BLOCK_METADATA } from '../../transformations.js';

// This module is a placeholder for transformation-specific UI components
// Most transformation UI functionality is currently in transformation-engine.js
// This separation allows for future UI-specific components that don't contain business logic